import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputSearchModal: {
    marginTop: 50,
    backgroundColor: "#262a2b",
    padding: 20,
    borderRadius: 15,
  },
  inputWrapper: {
    flexDirection: "row",
    alignItems: "center",
  },
  input: {
    fontSize: 22,
    color: "grey",
    marginLeft: 10,
  },
});
