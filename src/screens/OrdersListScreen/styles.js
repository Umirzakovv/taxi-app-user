import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    padding: 20,

  },

  title: {
    fontSize: 30,
    fontWeight: "bold",
    textAlign: "center",
    color: "#fff",
    marginVertical: 20
  },
});
